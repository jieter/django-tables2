django-haystack>=2.6.1
# mocks/stubs for tests
fudge
# xml parsing
lxml
pytz>0
# pin pytest to not use 3.3 as it does not support python 3.3 anymore.
pytest==3.2.5
pytest-django
pytest-cov
tablib==0.11.4
